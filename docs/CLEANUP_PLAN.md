# 🧹 AutentiZity — Deep Cleanup Plan

> **Generated**: June 26, 2026  
> **Scope**: Full codebase audit — unused files, dead code, duplicate deps, stale artifacts, and tech debt.

---

## 📊 Summary

| Category | Count |
|----------|-------|
| Safe to delete | 14 items |
| Needs verification | 5 items |
| Keep but clean | 8 items |
| Console.log statements | 29 sites (3 noisy) |
| Unused dependencies | 3 |
| Duplicate dependency | 1 |
| Legacy redirects (worth keeping) | 3 |

---

## 🗑️ SAFE TO DELETE

### 1. `.hermes/` — Historical AI plan directory
- **Path**: `.hermes/plans/2026-06-10-image-upload.md`
- **What**: A task plan generated by AI for the image upload feature. The feature was completed June 2026.
- **Action**: Delete the entire `.hermes/` tree.
- **Effort**: Small

### 2. `3revision/` — Historical revision notes
- **Path**: `3revision/dudas.md`, `3revision/hechos.md`
- **What**: Notes from the 3rd client revision. All 23 tasks completed. Superseded by `docs/CAMBIOS-APLICADOS.md`.
- **Action**: Delete the entire `3revision/` tree.
- **Effort**: Small

### 3. `design/` — Reference design assets (stale)
- **Path**: `design/notes.md`, `design/README.md`, `design/references/README.md`, `design/assets/README.md`
- **What**: Early design documentation. The color palette notes are inaccurate (old colors `#965458`, `#D6BEB7` instead of current `#E8788A`, `#E8C547`). Font notes mention P22 Mackinac and Chulapa fonts that were never implemented. Actual design lives in `src/app/globals.css` and `src/app/layout.tsx`.
- **Action**: Delete the entire `design/` tree.
- **Effort**: Small

### 4. `docs/CAMBIOS-APLICADOS.md` — One-time change log
- **Path**: `docs/CAMBIOS-APLICADOS.md`
- **What**: Detailed changelist from a June 9 client review. All tasks marked complete. Useful as history but not needed for ongoing development.
- **Action**: Delete (or archive outside the repo).
- **Effort**: Small

### 5. `docs/paginas/` — Screenshots (12 PNGs)
- **Path**: `docs/paginas/pagina_1.png` through `pagina_12.png`
- **What**: Screenshots of web pages for client review. Stale, no longer referenced, take up space.
- **Action**: Delete the entire `docs/paginas/` tree.
- **Effort**: Small

### 6. `docs/cliente-revision.html` — Client review file
- **Path**: `docs/cliente-revision.html`
- **What**: HTML file for client review. Historical, not referenced by the app.
- **Action**: Delete.
- **Effort**: Small

### 7. Unused public images
- **Paths**:
  - `public/images/logo-full.png` (6250×6250px — very large, unreferenced)
  - `public/images/logo.png`
  - `public/images/logo.svg`
  - `public/images/logo-full-transparent.png`
- **What**: Only `public/images/logo-transparent.png` is referenced in source code (used in Footer, Header, login, ranking, admin layout, and detail pages). The other 4 logo variants are unused.
- **Action**: Delete `logo-full.png`, `logo.png`, `logo.svg`, `logo-full-transparent.png`.
- **Effort**: Small (frees significant space from 6250×6250 PNG)

### 8. Empty `.gitkeep` placeholder files
- **Paths**:
  - `src/components/eventos/.gitkeep` (directory is empty except this)
  - `src/components/noticias/.gitkeep` (directory is empty except this)
  - `src/hooks/.gitkeep` (entire `hooks/` dir is empty)
  - `src/components/admin/.gitkeep` (dir has actual files)
  - `src/components/layout/.gitkeep` (dir has actual files)
  - `src/components/ui/.gitkeep` (dir has actual files)
  - `src/lib/data/.gitkeep` (dir has actual files)
  - `public/images/.gitkeep` (dir has actual files)
- **Action**: Delete all `.gitkeep` files. Three directories will become empty (`src/components/eventos/`, `src/components/noticias/`, `src/hooks/`) — decide whether to delete those directories too.
- **Effort**: Small

### 9. `src/components/eventos/` — Empty directory
- **Path**: `src/components/eventos/`
- **What**: Only contains `.gitkeep`. No components written.
- **Action**: Delete the directory (after removing .gitkeep).
- **Effort**: Small

### 10. `src/components/noticias/` — Empty directory
- **Path**: `src/components/noticias/`
- **What**: Only contains `.gitkeep`. No components written.
- **Action**: Delete the directory (after removing .gitkeep).
- **Effort**: Small

### 11. `src/hooks/` — Empty directory
- **Path**: `src/hooks/`
- **What**: Only contains `.gitkeep`. No custom hooks written.
- **Action**: Delete the directory (after removing .gitkeep).
- **Effort**: Small

### 12. Unused dependencies in `package.json`
- **`date-fns`** (`^4.1.0`): Not imported anywhere in source code. (0 references found via grep)
- **`date-fns-tz`** (`^3.2.0`): Not imported anywhere in source code. (0 references found via grep)
- **`sanitize-html`** (`^2.17.5`): Not imported anywhere in source code. The `renderRichText()` utility in `src/lib/utils.ts` uses custom `escapeHtml()` instead. The `@types/sanitize-html` devDependency is also unused.
- **Action**: Remove `date-fns`, `date-fns-tz`, `sanitize-html`, and `@types/sanitize-html` from `package.json`, then run `pnpm install`.
- **Effort**: Small

### 13. Build artifacts (already gitignored, still on disk)
- **Paths**: `.next/`, `tsconfig.tsbuildinfo`, `next-env.d.ts`, `.vercel/`
- **What**: These are already in `.gitignore`. Clean them from local filesystem.
- **Action**: Run `rm -rf .next tsconfig.tsbuildinfo .vercel/` (next-env.d.ts will regenerate on `next dev`). Add to a `clean` npm script for convenience.
- **Effort**: Small

### 14. Duplicate `react-advanced-cropper` in `package.json`
- **Path**: `package.json` lines 31-32
- **What**: The dependency is listed twice identically:
  ```json
  "react-advanced-cropper": "^0.20.1",
  "react-advanced-cropper": "^0.20.1",
  ```
- **Action**: Remove the duplicate line 32. Run `pnpm install` to clean lockfile.
- **Effort**: Small

---

## ❓ NEEDS VERIFICATION

### 15. `MPG_BE_Logo_SS_STK_WHT-1.webp` — Root-level logo file
- **Path**: `/MPG_BE_Logo_SS_STK_WHT-1.webp` (project root)
- **Referenced at**: `src/app/(public)/ranking/page.tsx:82`  
  `<img src="/MPG_BE_Logo_SS_STK_WHT-1.webp" alt="ManpowerGroup" ... />`
- **Issue**: The file is at the project root, not in `/public/`. Next.js serves files from `/public/` at the root URL path, so `/MPG_BE_Logo_SS_STK_WHT-1.webp` would not be served correctly unless it's in `public/`. However, the file is in the root (not `public/`), meaning this may be a broken image link.
- **Action**: Move the file to `public/MPG_BE_Logo_SS_STK_WHT-1.webp` and verify it loads in the ranking page.
- **Effort**: Small

### 16. Legacy redirect routes — keep or remove?
- **Paths**:
  - `src/app/(public)/eventos/page.tsx` → redirects to `/actividad`
  - `src/app/(public)/actividades/page.tsx` → redirects to `/actividad/actividades`
  - `src/app/(public)/movimientos/page.tsx` → redirects to `/actividad/movimientos`
- **What**: These are HTTP 307 redirects to the new `/actividad` hub page. The old slug pages at `/eventos/[slug]`, `/movimientos/[slug]`, `/actividades/[slug]` are still needed for detail views, but the index pages are now redirects.
- **Decision**: **Keep them.** They provide backward compatibility for any bookmarked URLs and preserve SEO value. They cost minimal maintenance.
- **Effort**: N/A (keep)

### 17. `src/lib/data/mock.ts` — Seed data still needed?
- **Path**: `src/lib/data/mock.ts`
- **Referenced by**: `src/app/api/db/seed/route.ts` only
- **What**: Contains 4 mock events and 3 mock news items with Unsplash placeholder images. The `GET /api/db/seed` endpoint uses this to seed the DB. Once production data is in the database, this file and the seed route are useless.
- **Decision**: **Keep for now.** Useful for development/staging environments. Consider deleting after production is fully seeded.
- **Effort**: Small (future)

### 18. `src/app/api/db/seed/route.ts` — One-time seed endpoint
- **Path**: `src/app/api/db/seed/route.ts`
- **What**: Seeds the database with mock data. One-time use for initial setup. No auth protection — anyone can hit the URL.
- **Decision**: **Keep but protect.** Either delete after production is seeded, or add admin auth check to prevent abuse.
- **Effort**: Small

### 19. `src/lib/data/sections.ts` + `/api/sections/` — Duplicate visibility system
- **Paths**: `src/lib/data/sections.ts`, `src/app/api/sections/route.ts`, `src/app/admin/secciones/page.tsx`
- **What**: This `sections` table manages section visibility on pages (toggle hero, stats, etc.). This is separate from `ecosistema_secciones` which manages ecosistema content sections. The naming is confusing — are both needed?
- **Decision**: **Keep but review.** Two different "sections" concepts with overlapping naming could be confusing. Consider renaming one to avoid confusion.
- **Effort**: Medium

---

## 🔧 KEEP BUT CLEAN

### 20. Duplicate `slugify` function
- **Locations**:
  - `src/lib/utils.ts:37` — exported `slugify()` (used by API POST routes)
  - `src/app/admin/ecosistema/page.tsx:634` — local `slugify()` inside `AdminEcosistemaPage`
- **Issue**: The admin/ecosistema page defines its own local `slugify` instead of importing from `@/lib/utils`.
- **Action**: Remove the local function and import from `@/lib/utils` instead.
- **Effort**: Small

### 21. Hardcoded `FALLBACK_SECTIONS` in ecosistema page
- **Path**: `src/app/(public)/ecosistema/page.tsx:57-105`
- **What**: Static fallback data with entity logos used when the DB has no ecosistema data. Contains hardcoded entity definitions that partially duplicate what the DB should have.
- **Decision**: **Keep until DB is seeded**, then remove the fallback and let the page show empty state when DB is empty.
- **Effort**: Small (future cleanup)

### 22. `globals.css` — Stale comments and font references
- **Path**: `src/app/globals.css:7,84`
- **Issues**:
  - Line 7: `[pendiente: CHULAPA para uppercase]` — CHULAPA was never implemented. Fonts are Google Fonts (Poppins + Montserrat).
  - Line 84: `/* Utilidades editorial — P22 Mackinac Pro para headings */` — P22 Mackinac was dropped due to licensing cost.
- **Action**: Update comments to match actual implementation.
- **Effort**: Small

### 23. `src/app/(public)/ecosistema/page.tsx` — Placeholder embajadores
- **Path**: Lines 215-222
- **What**: When there are no embajador entities in the DB, the page shows 8 "Embajador/a N — Próximamente" placeholder cards.
- **Decision**: **Keep.** This is intentional UX. No action needed.
- **Effort**: N/A

### 24. `design/notes.md` — Stale design references
- **Already covered in item #3** (delete entire `design/` tree).

### 25. `AI_CONTEXT.md` — References to non-existent files
- **Path**: `AI_CONTEXT.md:56`
- **Issue**: References `react-easy-crop` (not installed, not in package.json). The actual library used is `react-advanced-cropper`. Also references `src/lib/crop.ts` which does not exist (crop logic is in `src/app/api/crop/route.ts` using sharp).
- **Action**: Update AI_CONTEXT.md to reflect actual tech stack.
- **Effort**: Small

### 26. `public/fonts/` — Referenced but doesn't exist
- **Referenced in**: `globals.css` (Chulapa comment), `design/notes.md` (Chulapa Bold/Regular/Light)
- **What**: The directory `public/fonts/` was planned for Chulapa font files but was never created. All fonts come from Google Fonts CDN.
- **Action**: Remove stale font references from comments/docs.
- **Effort**: Small

### 27. Console.log statements — 29 sites
- **Error-level (26)**: Proper error logging in API routes and data fetches. These are fine for production debugging.
- **Warning-level (3)**: `src/components/admin/ImageUpload.tsx:40,50,59`  
  These warn about "coordenadas del recorte" (localStorage read/write/delete failures). These are verbose for production. They fire on every image upload, not just errors.
- **Action**: Downgrade the 3 `console.warn` calls in ImageUpload.tsx or gate them behind `process.env.NODE_ENV === "development"`.
- **Effort**: Small

---

## 📋 UNUSED TYPES

### 28. `NavItem` interface — Not imported anywhere
- **Path**: `src/lib/types.ts:93-97`
- **Exported**: `NavItem`, with `label`, `href`, `children?: NavItem[]`
- **Used by**: Nothing. Not imported in any file.
- **Action**: Remove from `types.ts`.
- **Effort**: Small

### 29. `AdminUser` interface — Not imported anywhere
- **Path**: `src/lib/types.ts:100-105`
- **Exported**: `AdminUser`, with `id`, `email`, `name`, `role`
- **Used by**: Nothing. Not imported in any file. Authentication is cookie-based with no user model.
- **Action**: Remove from `types.ts`.
- **Effort**: Small

---

## 📊 PUBLIC ASSETS AUDIT

All 12 files in `public/logos/` are referenced in the ecosistema page fallback data:

| File | Status |
|------|--------|
| `manpower.svg` | Referenced |
| `corteingles.svg` | Referenced |
| `msd.svg` | Referenced |
| `haleon.svg` | Referenced |
| `legalitas.svg` | Referenced |
| `papageno.png` | Referenced |
| `fundaciononce.png` | Referenced |
| `itgetsbetter.png` | Referenced |
| `trabajandoenpositivo.png` | Referenced |
| `aytomadrid.png` | Referenced |
| `icam.png` | Referenced |
| `ccce.png` | Referenced |

All `public/images/` files:

| File | Status |
|------|--------|
| `logo-transparent.png` | Used (header, footer, login, detail pages, ranking, admin layout) |
| `logo-full.png` | **UNUSED** |
| `logo.png` | **UNUSED** |
| `logo.svg` | **UNUSED** |
| `logo-full-transparent.png` | **UNUSED** |

---

## 🎯 PRIORITY ORDER (Recommended)

| Priority | Item | Effort | Impact |
|----------|------|--------|--------|
| **P0** | Fix duplicate `react-advanced-cropper` in package.json (#14) | Small | Prevents install issues |
| **P0** | Remove unused npm deps: `date-fns`, `date-fns-tz`, `sanitize-html`, `@types/sanitize-html` (#12) | Small | Smaller bundle, fewer CVEs |
| **P0** | Move `MPG_BE_Logo_SS_STK_WHT-1.webp` to `public/` (#15) | Small | Fixes broken image on ranking page |
| **P1** | Delete `.hermes/`, `3revision/`, `design/` directories (#1,2,3) | Small | Reduces noise |
| **P1** | Delete unused public images (#7) | Small | Frees disk space |
| **P1** | Delete `.gitkeep` files + empty dirs (#8,9,10,11) | Small | Cleaner tree |
| **P1** | Remove unused types: `NavItem`, `AdminUser` (#28,29) | Small | Cleaner types |
| **P2** | Update `AI_CONTEXT.md` — fix library names (#25) | Small | Accurate docs |
| **P2** | Remove stale design docs (#4,5,6) | Small | Less clutter |
| **P2** | Clean up `globals.css` comments (#22) | Small | Less confusion |
| **P2** | Gate `console.warn` in ImageUpload (#27) | Small | Cleaner production logs |
| **P3** | Deduplicate `slugify` function (#20) | Small | DRY |
| **P3** | Review `sections` vs `ecosistema_secciones` naming (#19) | Medium | UX clarity |
| **P3** | Remove hardcoded `FALLBACK_SECTIONS` after DB is seeded (#21) | Small | Future |
| **P3** | Delete `mock.ts` and `/api/db/seed` after production is live (#17,18) | Small | Future |

---

## 🚫 DELIBERATELY KEPT

These items were investigated and determined to be worth keeping:

| Item | Reason |
|------|--------|
| `src/app/(public)/eventos/page.tsx` redirect | Backward compatibility + SEO |
| `src/app/(public)/actividades/page.tsx` redirect | Backward compatibility + SEO |
| `src/app/(public)/movimientos/page.tsx` redirect | Backward compatibility + SEO |
| `src/app/api/db/setup/route.ts` | Required for DB schema initialization |
| `src/app/api/db/status/route.ts` | Required for DB diagnostics |
| `src/lib/data/cleanup.ts` | Actively used by 4 DELETE/PUT handlers |
| 26 `console.error` statements | Proper error logging for production debugging |
| `public/logos/*` (all 12 files) | All referenced in ecossistema fallback data |
| `public/images/logo-transparent.png` | Extensively referenced across layout and pages |

---

## 📝 RECOMMENDED GITIGNORE ADDITIONS

Already well-configured. No additions needed:
- `.next/` ✅
- `tsconfig.tsbuildinfo` ✅
- `next-env.d.ts` ✅
- `.vercel/` ✅
- `/inbox/*` ✅
